import React from "react";

// 订阅了一个上下文，获取最基本的
export default class ImportDemo extends React.Component {
    render() {
        return (
            <p>
                我是延迟加载的组件
            </p>
        )
    }
}